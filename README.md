# Symfony REST-приложение для расчета цены продукта и проведения оплаты

Сделано по тестовому заданию: https://github.com/systemeio/backend-test-task

## 🛠️ Стек технологий
- **PHP** 8.3
- **Symfony** 6.4
- **MySQL** 8.0
- **Docker** & **Docker Compose**
- Тесты **PHPUnit**

## 📋 Требования (Prerequisites)
- Docker
- Docker Compose
- Make (опционально, но желательно для удобства)
- Для запуска в Windows нужен WSL2

## 🚀 Установка и запуск
- `git clone https://github.com/KharchenkoDev/systemeio-backend-test-task.git`
- `cd systemeio-backend-test-task/`
- `make init`
- `make fixtures`

Приложение будет доступно по адресу: http://localhost:8337

## ✅ Тесты

- `make test`
- Запросы к API можно отправить с помощью файла `./requests.http` (В VSCode можно использовать расширение **REST Client**)

## ⚙️ API

1. POST: для расчёта цены

http://127.0.0.1:8337/calculate-price

Пример JSON тела запроса:
```
{
    "product": 1,
    "taxNumber": "DE123456789",
    "couponCode": "SALE15PERCENT"
}
```
Возвращает цену с учётом налога и скидки:
```
{
  "success": true,
  "price": 101.15
}
```

2. POST: для осуществления покупки

http://127.0.0.1:8337/purchase

Пример JSON тела запроса:
```
{
    "product": 1,
    "taxNumber": "IT12345678900",
    "couponCode": "SALE10AMOUNT",
    "paymentProcessor": "paypal"
}
```
Вычисляет цену, вызывает API платёжного сервиса, возвращает вычисленную цену:
```
{
  "success": true,
  "price": 109.8
}
```
## 🧩 Некоторые особенности реализации

- Десериализация и валидация входящих данных выполняется средствами фреймворка с использованием атрибута `#[MapRequestPayload]`
- Работа с классами платёжных сервисов реализована через адаптеры (чтобы привести их к единому интерфейсу).
- Выбор платёжного сервиса производится с помощью провайдера стратегий. Реестр сервисов создаётся автоматически благодаря атрибуту `#[AutowireIterator]`, который автоматически собирает сервисы имплементирующие интерфейс адаптера (привязка с помощью тегов сервисов).
- Таким образом, для подключения нового платёжного сервиса достаточно добавить новый адаптер. Такой сервис сразу добавится и в валидатор (который проверяет перечень допустимых строк в поле **paymentProcessor**), и в реестр стратегий.
- В рамках рабочего проекта следовало бы доработать систему возвращения ошибок, но для тестовой задачи оставил в упрощённом виде.



